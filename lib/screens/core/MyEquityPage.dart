import 'package:flutter/cupertino.dart';

class MyEquityPage extends StatelessWidget {
  const MyEquityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('EQ'),
      ),
    );
    throw UnimplementedError();
  }
}