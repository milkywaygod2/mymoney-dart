import 'package:flutter/cupertino.dart';

class MyBalanceSheetPage extends StatelessWidget {
  const MyBalanceSheetPage({super.key});

  @override Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('BS'),
      ),
    );
    throw UnimplementedError();
  }
}