import 'package:flutter/cupertino.dart';

class MyProfitLossPage extends StatelessWidget {
  const MyProfitLossPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('PL'),
      ),
    );
    throw UnimplementedError();
  }
}