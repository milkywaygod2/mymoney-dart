import 'package:flutter/cupertino.dart';

class MyLink extends StatelessWidget {
  const MyLink({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('OuterLink'),
      ),
    );
    throw UnimplementedError();
  }
}